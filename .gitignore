# Environment and secrets
.env
.env.*
!.env.example
token_1.py

# Local run state (batch IDs, candidate caches) — not repo content
.*last_batch_id

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
.venv/
env/
.env/
talent_radar/

# Symlinked external folders
LLM-wiki-vault

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

*.xlsx
*.csv
*.json
*.log

# Generated candidate viewers (embed real candidate PII as inline JSON) — same
# reason as the csv/xlsx exclusion above, just a different output format.
*viewer.html
data.json.js

# OS junk / local shortcuts
.DS_Store
/Claude

# Nested repos with their own remote — back up via their own git push,
# not as a gitlink here. pb-daily-dashboard is company-owned (org repo),
# excluded outright, not just deduped.
/phantombuster-api/
/coding_sourcer/
/pb-daily-dashboard/
/Teamtailor-sourced-candidates-per-job/

# Local-only, purged from history 2026-09-16 — keep on disk for VS Code, never push.
/Google-Maps-Lycees-France/
/Agent Pierre-Richard DUPONT (PRD)/
/Teamtailor feedback extractions/
/Teamtailor-make-weekly-analytics/
/Teamtailor_candidate_profile_tool/
/Teamtailor_quaterly_extraction/
/X personal branding enricher/
/Product Manager/

# Medium article drafts — written for Medium.com, never for GitHub.
*[Mm]edium*.md
