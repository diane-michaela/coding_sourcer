# Make module 1 ("Set jobs list") — code:ExecuteCode, Python
# Scenario: TeamTailor-report (diane) — https://eu1.make.com/67084/scenarios/5316616/edit
#
# Outputs the list of {job_id, channel} pairs that module 40 (BasicFeeder)
# iterates over. Currently hardcoded — see README.md for why this is the
# scenario's main weak point (and why it "doesn't run" right now).

return [
  {"job_id": "7463157", "channel": "#product-expert-recruitment-2026"},

]
